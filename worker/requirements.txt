# Install system-wide (no virtualenv):
#   pip install -r requirements.txt --break-system-packages
# Also requires system packages:
#   apt install ffmpeg fonts-vazirmatn
faster-whisper>=1.0.0
boto3>=1.34.0
